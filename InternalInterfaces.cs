using Nickel;

namespace TheJazMaster.Louis;

internal interface ILouisCard
{
	static abstract void Register(IModHelper helper);
}

internal interface ILouisArtifact
{
	static abstract void Register(IModHelper helper);
}